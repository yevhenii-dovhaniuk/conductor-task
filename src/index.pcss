body {
    margin: 0;
    padding: 0;
    font-family: sans-serif;
}

html,
body {
    height: 100%;
    background: #f7f7f7;
}

.content {
    display: flex;
    justify-content: center;

    &__wrapper {
        flex: 0 0.8 1280px;
        margin: 20px 0;
    }
}
